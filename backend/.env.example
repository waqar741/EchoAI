# ═══════════════════════════════════════════════════════════════
# Voice Avatar Backend - Environment Configuration
# ═══════════════════════════════════════════════════════════════
# Copy this file to .env and fill in the values

# ── LLM Configuration ──
LLM_API_URL=your_llm_api_url_here
LLM_MODEL=Qwen2.5-1.5B-Instruct
LLM_MAX_TOKENS=150
LLM_TEMPERATURE=0.7

# ── Server Configuration ──
# Development: 0.0.0.0 (all interfaces)
# Production: 127.0.0.1 (localhost only, behind reverse proxy)
HOST=127.0.0.1
PORT=8000

# ── CORS Origins ──
# Comma-separated list of allowed origins
# Development: http://localhost:5173,http://localhost:3000
# Production: https://yourdomain.com,https://www.yourdomain.com
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ── Rate Limiting ──
# Requests per minute per IP (protection against abuse)
RATE_LIMIT=30/minute
